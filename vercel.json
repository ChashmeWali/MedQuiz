{
    "builds": [{
        "src": "MedQuiz/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "MedQuiz/wsgi.py"
        }
    ]
}